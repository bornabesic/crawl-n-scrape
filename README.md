# Crawl 'n' Scrape
```
usage: crawl_n_scrape.py [-h] definition_dir time_delay

positional arguments:
  definition_dir  Name of directory containing def.json and Parser.py
  time_delay      Time delay (in seconds) between each web page access

optional arguments:
  -h, --help      show this help message and exit
  ```
